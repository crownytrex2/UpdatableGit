-- Destroy existing GUI if re-run
pcall(function() game.CoreGui:FindFirstChild("CrownyGUI"):Destroy() end)

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local player = Players.LocalPlayer

-- Main ScreenGui
local gui = Instance.new("ScreenGui", game.CoreGui)
gui.Name = "CrownyGUI"
gui.ResetOnSpawn = false

-- Main Frame
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0, 300, 0, 260)
frame.Position = UDim2.new(0.5, -150, 0.5, -130)
frame.AnchorPoint = Vector2.new(0.5, 0.5)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BackgroundTransparency = 0.1
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

-- Title Bar (shifted left)
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1, -90, 0, 30)
title.Position = UDim2.new(0, 10, 0, 0)
title.Text = "CROWNYTREX GUI"
title.BackgroundTransparency = 1
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 16

-- Minimize Button
local minimizeBtn = Instance.new("TextButton", frame)
minimizeBtn.Size = UDim2.new(0, 80, 0, 25)
minimizeBtn.Position = UDim2.new(1, -85, 0, 2)
minimizeBtn.Text = "Minimize"
minimizeBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
minimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeBtn.Font = Enum.Font.Gotham
minimizeBtn.TextSize = 14
Instance.new("UICorner", minimizeBtn).CornerRadius = UDim.new(0, 5)

-- Tab Buttons (ABUSE, TROLL, FUNNY)
local tabsFrame = Instance.new("Frame", frame)
tabsFrame.Size = UDim2.new(1, -20, 0, 30)
tabsFrame.Position = UDim2.new(0, 10, 0, 35)
tabsFrame.BackgroundTransparency = 1
local tabNames = {"ABUSE", "TROLL", "FUNNY"}
local tabButtons, tabPanels = {}, {}

for i, name in ipairs(tabNames) do
    local btn = Instance.new("TextButton", tabsFrame)
    btn.Size = UDim2.new(1/3, -4, 1, 0)
    btn.Position = UDim2.new((i-1)/3, 4*(i-1), 0, 0)
    btn.Text = name
    btn.Name = name .. "TabBtn"
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.Gotham
    btn.TextSize = 14
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
    tabButtons[name] = btn

    local panel = Instance.new("Frame", frame)
    panel.Size = UDim2.new(1, -20, 1, -80)
    panel.Position = UDim2.new(0, 10, 0, 70)
    panel.BackgroundTransparency = 1
    panel.Visible = (i == 1) -- default: show ABUSE
    tabPanels[name] = panel
end

-- Example Buttons for each tab (you can add more)
local function createTabButton(panel, txt)
    local b = Instance.new("TextButton", panel)
    b.Size = UDim2.new(0, 120, 0, 30)
    b.Position = UDim2.new(0, 10 + (#panel:GetChildren()-1)*130, 0, 10)
    b.Text = txt
    b.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
    b.TextColor3 = Color3.fromRGB(255, 255, 255)
    b.Font = Enum.Font.Gotham
    b.TextSize = 14
    Instance.new("UICorner", b).CornerRadius = UDim.new(0, 5)
    return b
end

-- ABUSE tab buttons
createTabButton(tabPanels["ABUSE"], "ABUSE 1").MouseButton1Click:Connect(function() print("Triggered ABUSE script") end)
createTabButton(tabPanels["ABUSE"], "ABUSE 2").MouseButton1Click:Connect(function() print("Triggered ABUSE 2") end)

-- TROLL tab buttons
createTabButton(tabPanels["TROLL"], "TROLL 1").MouseButton1Click:Connect(function() print("Triggered TROLL script") end)
createTabButton(tabPanels["TROLL"], "TROLL 2").MouseButton1Click:Connect(function() print("Triggered TROLL 2") end)

-- FUNNY tab buttons
createTabButton(tabPanels["FUNNY"], "FUNNY 1").MouseButton1Click:Connect(function() print("Triggered FUNNY script") end)
createTabButton(tabPanels["FUNNY"], "FUNNY 2").MouseButton1Click:Connect(function() print("Triggered FUNNY 2") end)

-- Tab switching logic
for name, btn in pairs(tabButtons) do
    btn.MouseButton1Click:Connect(function()
        for tName, panel in pairs(tabPanels) do
            panel.Visible = (tName == name)
        end
    end)
end

-- Dragging logic
local dragging, dragInput, dragStart, startPos
title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = frame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
title.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
                                   startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Minimize logic (hide tabs & panels)
local contentItems = {tabsFrame, unpack(tabPanels)}
local isMinimized = false
minimizeBtn.MouseButton1Click:Connect(function()
    isMinimized = not isMinimized
    for _, item in ipairs(contentItems) do
        item.Visible = not isMinimized
    end
end)

-- Transparency slider (outside tabs)
local slider = Instance.new("TextButton", frame)
slider.Size = UDim2.new(0.9, 0, 0, 30)
slider.Position = UDim2.new(0.05, 0, 1, -60)
slider.Text = "Transparency: 10%"
slider.BackgroundColor3 = Color3.fromRGB(70, 70, 70)
slider.TextColor3 = Color3.fromRGB(255, 255, 255)
slider.Font = Enum.Font.Gotham
slider.TextSize = 14
Instance.new("UICorner", slider).CornerRadius = UDim.new(0, 5)
slider.MouseButton1Click:Connect(function()
    frame.BackgroundTransparency = frame.BackgroundTransparency + 0.1
    if frame.BackgroundTransparency > 1 then frame.BackgroundTransparency = 0 end
    slider.Text = "Transparency: " .. math.floor((1 - frame.BackgroundTransparency)*100) .. "%"
end)

-- Fixed fly button in FUNNY tab (example)
local flyButton = createTabButton(tabPanels["FUNNY"], "Toggle Fly")
flyButton.MouseButton1Click:Connect(function()
    -- put your fly code here...
    print("Fly toggled from FUNNY!")
end)

-- Permanent watermark
local note = Instance.new("TextLabel", gui)
note.Size = UDim2.new(0, 300, 0, 30)
note.Position = UDim2.new(1, -310, 1, -40)
note.BackgroundTransparency = 0.2
note.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
note.Text = "MADE BY CROWNYTREX ON DISCORD"
note.TextColor3 = Color3.fromRGB(255, 255, 255)
note.Font = Enum.Font.GothamBold
note.TextSize = 14
Instance.new("UICorner", note).CornerRadius = UDim.new(0, 6)
